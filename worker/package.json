{
  "name": "madewith-scraper",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that scrapes GitHub on a cron and refreshes the MadeWith… catalog data.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "tsx": "^4.23.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.110.0"
  }
}
