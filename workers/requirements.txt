# Shared dependency set for every worker under workers/.
#
# One virtualenv (workers/.venv) serves projects, videos, posts, and utils.
# The three former per-worker venvs shared 21 packages with zero version
# conflicts, and projects' requirements were a strict subset of videos', so
# splitting them bought nothing but three copies of Scrapy.
#
#   python3 -m venv workers/.venv
#   workers/.venv/bin/pip install -r workers/requirements.txt

# Crawlers — projects (github) and videos (youtube)
scrapy>=2.11,<3
psycopg[binary]>=3.1
python-dotenv>=1.0

# videos — transcript fetch and LLM enrichment
youtube-transcript-api==1.2.4
openai>=1.0.0

# posts — editorial engine (GitHub facts, cover/data image generation)
requests>=2.32.0
python-dateutil>=2.9.0
Pillow>=10.0.0

# utils — S3 static-site deploy
boto3>=1.34

# github — discovery/enrichment service. Installed editable so its console
# script (madewith-github) lands in workers/.venv/bin:
#   workers/.venv/bin/pip install -e workers/github
httpx>=0.27,<1
psycopg[binary,pool]>=3.2,<4
pydantic>=2.8,<3
pydantic-settings>=2.4,<3
typer>=0.12,<1
tenacity>=9,<10
orjson>=3.10,<4
