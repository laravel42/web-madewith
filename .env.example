# Optional: a GitHub token lifts the scraper from 60 → 5,000 requests/hour and
# unlocks the real per-repo language breakdown (unauthenticated runs synthesise
# it from the primary language). A classic token with no scopes, or a
# fine-grained token with public read access, is enough.
GITHUB_TOKEN=
