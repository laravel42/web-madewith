{
  "schemaVersion": 3,
  "videoId": "EA37aMqMwc0",
  "language": "en",
  "source": "auto",
  "seoDescription": "Explore SolidJS, a high-performance JavaScript framework known for its developer ergonomics and impressive speed. This guide walks you through setting up a new SolidJS project with TypeScript and Vite, explaining the core concepts of reactive state management using `createSignal`. Learn how SolidJS handles JSX with a custom Babel transform for optimal performance, and see a practical example of building a simple counter application. Discover SolidJS's elegant approach to global state management without complex libraries.",
  "chapters": [
    {
      "title": "Introduction to SolidJS",
      "description": "This chapter introduces SolidJS as a modern JavaScript framework with great developer ergonomics and growing momentum.",
      "startTime": 0.0,
      "endTime": 23.0,
      "slug": "introduction-to-solidjs"
    },
    {
      "title": "Project Setup with Vite and TypeScript",
      "description": "This chapter demonstrates how to create a new SolidJS project using adegit template that includes TypeScript support.",
      "startTime": 23.0,
      "endTime": 56.0,
      "slug": "project-setup-with-vite-and-typescript"
    },
    {
      "title": "Exploring the Project Configuration",
      "description": "This chapter covers an overview of the package.json file, highlighting SolidJS's minimal dependencies, its use of Vite, and the key npm scripts.",
      "startTime": 56.0,
      "endTime": 124.0,
      "slug": "exploring-the-project-configuration"
    },
    {
      "title": "Running the Default Application",
      "description": "This chapter examines the project's entry point, runs the application for the first time, and demonstrates hot reloading.",
      "startTime": 124.0,
      "endTime": 187.0,
      "slug": "running-the-default-application"
    },
    {
      "title": "Reactive State with `createSignal`",
      "description": "This chapter introduces SolidJS's core reactivity primitive, createSignal, to build a simple interactive counter application.",
      "startTime": 187.0,
      "endTime": 290.0,
      "slug": "reactive-state-with-createsignal"
    },
    {
      "title": "How SolidJS Handles JSX",
      "description": "This chapter explains how SolidJS uses a custom Babel transform to compile JSX into highly efficient code, and the benefits this provides.",
      "startTime": 290.0,
      "endTime": 404.0,
      "slug": "how-solidjs-handles-jsx"
    },
    {
      "title": "Global State Management with Modules",
      "description": "This chapter refactors the application to show how SolidJS can manage shared state across components simply by using JavaScript modules.",
      "startTime": 404.0,
      "endTime": 541.0,
      "slug": "global-state-management-with-modules"
    },
    {
      "title": "The Power of Hot Module Replacement",
      "description": "This chapter demonstrates how SolidJS's architecture, combined with Vite, preserves application state during development even when UI components are updated.",
      "startTime": 541.0,
      "endTime": 587.0,
      "slug": "the-power-of-hot-module-replacement"
    },
    {
      "title": "Building and Serving for Production",
      "description": "This chapter explains how to use the built-in npm scripts to create an optimized production build and serve it locally for testing.",
      "startTime": 587.0,
      "endTime": 618.0,
      "slug": "building-and-serving-for-production"
    },
    {
      "title": "Final Thoughts and Performance",
      "description": "This chapter concludes the overview, highlighting SolidJS's impressive performance and encouraging viewers to try it out.",
      "startTime": 618.0,
      "endTime": 655.88,
      "slug": "final-thoughts-and-performance"
    }
  ],
  "summary": "## Getting Started with SolidJS\n\nCreating a new SolidJS project is simple, especially with official templates that support TypeScript out of the box. This tutorial uses Vite as the build tool.\n\n```bash\n# Create a new TypeScript project\nnpx degit solidjs/templates/ts my-solid-app\n\n# Navigate into the project and install dependencies\ncd my-solid-app\nnpm install\n\n# Start the development server\nnpm start\n```\n\n## Core Concepts: Signals\n\nReactivity in SolidJS is built on **signals**. They are the foundation for creating dynamic and efficient user interfaces.\n\n- **`createSignal(initialValue)`**: This function creates a new reactive state variable.\n- **Return Value**: It returns a tuple containing a **getter** and a **setter** function: `const [count, setCount] = createSignal(0);`\n- **Reading State**: To get the current value, you must call the getter function: `console.log(count());`\n- **Updating State**: To change the value and trigger UI updates, you call the setter function: `setCount(count() + 1);`\n\n> [!IMPORTANT]\n> When you read a signal's value within a component or effect, you create a subscription. SolidJS automatically re-runs the relevant code when that signal's value changes.\n\n## Components and JSX\n\nSolidJS uses a familiar component-based architecture with **JSX/TSX**, providing excellent developer ergonomics and type-safety.\n\n- **No Virtual DOM**: Unlike React, SolidJS compiles JSX into highly optimized, direct DOM manipulation code. Components are regular functions that run once to set up the view and reactivity, not on every state change.\n- **Fine-Grained Reactivity**: When a signal updates, SolidJS only updates the specific parts of the DOM that depend on that signal, making it incredibly fast.\n\n## State Management\n\nSolidJS's reactivity model enables a powerful and simple approach to state management that often eliminates the need for complex libraries.\n\n- **Local State**: Use `createSignal` inside components for local state.\n- **Shared/Global State**: To share state across the application, you can create signals in a separate module (`.ts` file) and export them.\n\n> [!TIP]\n> By placing signals in a standalone module, any component can import and use them directly. This avoids prop-drilling and the complexity of context providers.\n\n**Example `store.ts`:**\n```typescript\nimport { createSignal } from \"solid-js\";\n\nconst [elephants, setElephants] = createSignal(0);\nconst increasePopulation = () => setElephants(elephants() + 1);\n\nexport { elephants, increasePopulation };\n```\n**Example Component:**\n```typescript\nimport { elephants } from './store';\n\nconst ElephantCount = () => {\n  return <div>Elephants: {elephants()}</div>;\n}\n```\n\n## Building for Production\n\nThe template provides standard scripts for development and production workflows:\n- **`npm run build`**: Bundles and optimizes the application into a `dist` directory.\n- **`npm run serve`**: Runs a local server to preview the production build.",
  "transcription": "## Introduction to SolidJS\n\nIf you are looking for a solid javascript framework, Solid.js is an excellent choice. I loved it when I looked at it ages ago. I loved the developer ergonomics it provided, and it's been gathering significant momentum in recent years. So if you are like me and like learning new tech, even if it's not something that you will immediately use in your day job, this lesson is for you. So let's go.\n\n## Project Setup with Vite and TypeScript\n\nCreating a new Solid JS project is pretty easy. It comes with its own templates and even provides a template for typescript. \n\nWe are running `npx degit`, which is essentially going to download the git project solid-js/templates and from that the `ts` folder, and extract it into this folder called `demo-solid`. And now we have a new SolidJS TypeScript project and we can simply `cd` into this directory and run `npm install`. Once the installation is complete, we can open it up in our favorite IDE, which is of course VS Code.\n\n## Exploring the Project Configuration\n\nNow the first thing that I want to showcase about Solid.js is something that you can find within the package.json. Notice that in order to use SolidJS, the only package that you really need is the solid-js package itself. Because Solid.js is written in TypeScript with TypeScript-first support, you don't have to install any type definitions, for example `@types/react`.\n\nThe next thing that I want to point out is not specific to Solid.js and is more specific to this particular template, and that is that it is using Vite as its module bundler. If you are new to Vite, I already have a few lessons up on that and I'll leave them in the description, but fundamentally this particular template is going to compile blazingly fast. Now you don't have to use Vite with Solid.js; any other module bundler like webpack will also work perfectly fine.\n\nAnd one last thing that I want to point out within the package.json is the scripts section. We have three key main scripts: we have the `start` script, which is going to be used during development, and the `dev` script is the same. We have the `build` script which is going to bundle our application so we can deploy to production, and we have the `serve` script which can be used to test the production build locally if you wish to do so. We will demo all of these in this lesson.\n\n## Running the Default Application\n\nNow the way Vite works is that it looks at our index.html and looks for any script tags that it might have and makes them our application entry points. Within our index.html we have the `src` attribute pointing to `src/index.tsx`, and that is our main application entry point. So if we go ahead and open that up within our IDE we find the usual `render` function which is rendering a simple `App` component. So the next step of course is to jump into the `App` component found within `App.tsx`.\n\nNow it is a pretty simple stateless component. So if you are familiar with any other TSX components, for example with React, this should be pretty familiar. So let's run this application by opening up our terminal and running `npm start`. And then we can visit this application within our browser and we can see this component being rendered on screen. Now it's prompting us to modify `src/App.tsx`. So let's jump back into our IDE and modify the edit text to be 'modify', and this will live reload within the browser and we can see that our text is updated within the component.\n\n## Reactive State with `createSignal`\n\nSo no surprises so far. Let's modify this to build a very basic web application. We get rid of all of the imports and then we only bring in the `Component` import from `solid-js` along with the `createSignal` function. `createSignal` is how you create atomic state variables within Solid.js, and now we can get rid of whatever the App component was rendering before.\n\nNow the first thing that we will do is we will create a state to store a number of elephants. We do this by calling the `createSignal` function, passing in an initial value which we are setting to 0. And this returns a tuple of two things: a function that we are storing in a variable called `elephants`, which we can invoke to get the current value of the signal, and a function which we have stored in `setElephants` which can be used to set a new value for the signal.\n\nNow let's create a utility function which increases the number of elephants by one. This will first use the current value of the signal by invoking `elephants` and then invoke `setElephants` by passing in `elephants() + 1`, thus incrementing our total elephant count.\n\nSolid.js works on the concept of actions and effects. You modifying a signal is essentially an action, and the effect is that your application is going to re-render with an updated DOM. You don't have to constantly think about these concepts because Solid.js is pretty much transparently reactive.\n\nNow for the rendering of our `App` component, we want to simply render a div that's going to display the current number of elephants, followed by a button that is going to add a new elephant to the room which we are going to point to the `increasePopulation` function. Because we are getting the current number of elephants by invoking the `elephants` signal, whenever the particular signal value is going to change, this UI is automatically going to be re-rendered by Solid.js. And if we jump back to the browser, we can see that in action. We can click the button to add a new elephant and the elephant count updates.\n\n## How SolidJS Handles JSX\n\nNotice that Solid.js works out of the box with JSX, or as I like to call it, TSX. This means that you get all of the great developer ergonomics that the TypeScript team has built for TSX without any of the compromises. And the only thing that Solid.js needs to do different in order to support JSX is to provide a custom Babel transform.\n\nLet's look at what I mean. We can actually take our application and put it into playground.solidjs.com. Of course our application behaves exactly the same, but the reason why I came over here is to show you the generated JavaScript output. You can see that the output pretty much matches our input. We still have the `const App`, we still have the two calls, but the only thing that is different is the return call in which JSX is replaced with something that is generated by the Solid.js transform.\n\nNow just like in the real world with React, you don't exactly need to know how individual JSX elements are transformed into `React.createElement`. You don't actually need to know exactly how these particular JSX elements are being transformed into various DOM elements. The fundamental thing that you need to know is that the JSX is going to be transformed by Solid.js into something that it can run again and again as you modify various things, for example, signals.\n\nAnd because the type checking is performed independently of the JSX transform, this means that you are not likely to run into any weird compile-time type checking issues that you could in other frameworks that either try to add additional TypeScript syntax like Svelte or try to support type checking for their own template languages like Angular. This is because type checking is an extremely hard problem that I feel is best solved by the TypeScript team which is dedicated for this purpose.\n\n## Global State Management with Modules\n\nAnd now let's look at how Solid improves in state management over React. As your application gets bigger and bigger and starts to scale to the moon, you find yourself wanting to create more and more reusable portions of your application. For example, think of an e-commerce application. We can think of the elephants as the number of items in our cart and the `increasePopulation` button as 'Add to Cart'. You might want to use it again and again, so we move them into their own custom components.\n\nWe can move the elephant count into its own custom component simply pasting in the div that we had before. We can move the add elephant button into its own custom component as well, simply pasting in the button that we had before. And now for the people that still want to use the elephant count and the add elephant button next to each other, we create this component called `Jungle` that simply renders out the elephant count and the elephant button. And this is something that we had previously in our `App.tsx`, that is the elephant count and the button, so we can just place in the `Jungle` component to essentially get the same functionality that we had before.\n\nAnd this brings us to the reason why state management libraries exist in any large scale application. Fundamentally our application state is not a one-to-one map of our application componentry. So at this point we have to figure out a way to get the `elephants` and the `increasePopulation` all the way through the `Jungle` into the `ElephantCount` and the `AddElephantButton`.\n\nFortunately with Solid.js, you don't actually need to have your effects as a part of your render tree. So we cut the two utilities that we have and we create a new JavaScript module called `app.store`. And we simply paste in the same code that we had before, and from this module export the `elephants` and the `increasePopulation` variables. And now we can call the `increasePopulation` method anywhere within our render tree and it will correctly update the elephant count. So we don't need to do anything fancy in order to get access to this particular method. Similarly, in order to get access to the current elephant signal value, we can simply bring in the `elephants` export from the `app.store` module. And now everything should work as expected. All of our files are without errors and there are no more red files.\n\n## The Power of Hot Module Replacement\n\nAnd because we had `npm start` running in the background, we can open up the browser and you can see that it still behaves exactly as it was doing before.\n\nNow, there are a lot of developers that like abstractions for the sake of abstractions, but I personally find that the simpler the code, the less likely you are to create a bug. Now one more thing that I want to point out is because we've moved the app store into its own module, if you make a modification to any other module, for example the `ElephantCount` by adding a cute little elephant emoji, only this particular `ElephantCount` module is going to get reloaded and the app store will get reused. This means that we can update our application UI without overwriting the previous state. So within the browser, you can see that the count is still 13 and we can continue to increment it in the new UI. And that's another signal of good software development. You know your abstractions are good if they result in simpler and less concepts.\n\n## Building and Serving for Production\n\nNow finally, as we mentioned when we looked at our package.json, when we are ready to build our application for production, we can run `npm run build`, and this builds our production app into the `dist` folder, which we can push to some hosting provider. And I have lessons for that already that I will link in the video description. But you can also test it locally by running `npm run serve`. This gives us this URL, `localhost:5000`, and you can see that our production app behaves exactly as it did during dev.\n\n## Final Thoughts and Performance\n\nThere is a lot more that goes into making a complete front-end development framework, and Solid.js is no exception. One thing that I do want to point out is that Solid.js is blazingly fast, in fact, almost as fast as vanilla JavaScript.\n\nAnd that's all for this lesson. It should be enough to get you started or at least excited. Smash that like and subscribe and I will see you in the next one."
}
