{
  "schemaVersion": 3,
  "videoId": "0C-y59betmY",
  "language": "en",
  "source": "auto",
  "seoDescription": "Ryan Carniato, creator of SolidJS, demystifies the core of JavaScript frameworks: DOM rendering. This guide breaks down the three fundamental approaches used by libraries like React, Vue, Svelte, and Angular. You'll learn the mechanics behind dirty checking (diffing template \"holes\"), the popular virtual DOM (VDOM) method, and the highly efficient fine-grained reactivity model. Understanding these rendering strategies is the key to mastering how modern JavaScript frameworks operate under the hood.",
  "chapters": [
    {
      "title": "Introduction to Declarative Rendering",
      "description": "The creator of SolidJS introduces the topic of DOM rendering in JavaScript frameworks and defines what makes them declarative.",
      "startTime": 0.0,
      "endTime": 51.0,
      "slug": "introduction-to-declarative-rendering"
    },
    {
      "title": "The Anatomy of a UI Template",
      "description": "This chapter breaks down declarative templates into their two fundamental parts: static content and dynamic expressions or \"holes\".",
      "startTime": 51.0,
      "endTime": 102.0,
      "slug": "the-anatomy-of-a-ui-template"
    },
    {
      "title": "The Inefficient `innerHTML` Method",
      "description": "An explanation of the obsolete rendering method of creating a string and setting it via `innerHTML`, demonstrating why it's inefficient.",
      "startTime": 102.0,
      "endTime": 208.0,
      "slug": "the-inefficient-innerhtml-method"
    },
    {
      "title": "The Problem with Direct DOM Diffing",
      "description": "This section explains why reading directly from the DOM to check for changes is also slow and can lead to performance bottlenecks.",
      "startTime": 208.0,
      "endTime": 289.0,
      "slug": "the-problem-with-direct-dom-diffing"
    },
    {
      "title": "Method 1: Dirty Checking (Diffing the Holes)",
      "description": "This chapter introduces the first modern rendering method, used by frameworks like Svelte and Lit, which only diffs the dynamic parts of a template.",
      "startTime": 289.0,
      "endTime": 551.0,
      "slug": "method-1-dirty-checking-diffing-the-holes"
    },
    {
      "title": "Method 1.5: The Virtual DOM",
      "description": "This chapter covers the most popular method, the Virtual DOM (used by React and Vue), which builds a JavaScript object representation to diff against.",
      "startTime": 551.0,
      "endTime": 788.0,
      "slug": "method-1-5-the-virtual-dom"
    },
    {
      "title": "Method 2: Fine-Grained Reactivity",
      "description": "The third and final method, popularized by SolidJS, forgoes diffing entirely by creating direct, reactive bindings from state to the DOM.",
      "startTime": 788.0,
      "endTime": 976.0,
      "slug": "method-2-fine-grained-reactivity"
    },
    {
      "title": "Summary and Trade-Offs",
      "description": "A concluding summary of the three rendering approaches, their primary trade-offs, and a recap of how all modern frameworks use one of these methods.",
      "startTime": 976.0,
      "endTime": 1015.079,
      "slug": "summary-and-trade-offs"
    }
  ],
  "summary": "## The Core of Declarative Frameworks\n\nUnderstanding how a declarative JavaScript framework **renders to the DOM** is the key to understanding the framework itself. All modern frameworks take **state** as input and produce **UI** as output, but they use different strategies to do so efficiently.\n\nAll declarative templates consist of two parts:\n- **Static Content**: The parts of the template that never change, like HTML tags and static text.\n- **Dynamic \"Holes\"**: The parts where state is injected, causing the UI to change when the state updates.\n\n> [!WARNING]\n> The simplest rendering approach—concatenating a string and using `innerHTML`—is highly inefficient because it tears down and recreates all DOM nodes on every update, even those that haven't changed.\n\n## The Three Rendering Methods\n\nThere are essentially three primary methods modern frameworks use to update the DOM without full recreation.\n\n### 1. Dirty Checking / Diffing the Holes\n\nThis method focuses only on the dynamic parts of the template.\n- **Pioneered by**: Angular. Used by Svelte, Lit, and Ember.\n- **How it works**: On the first render, the framework parses the template to create the static DOM structure and identifies the \"holes.\" It creates **bindings** for these holes, often using placeholder comment nodes.\n- **On Update**: The framework re-runs the template function to get the new values for the holes. It then **diffs** only these new values against the old values stored in the bindings and applies minimal updates directly to the DOM (e.g., updating a text node's value).\n\n### 2. The Virtual DOM (VDOM)\n\nThis is a variation of dirty checking and is the most popular approach.\n- **Pioneered by**: React. Used by Vue, Preact, and many others.\n- **How it works**: On every render, the framework executes a function (like `createElement` or `h`) that builds a complete, lightweight JavaScript object representing the desired DOM tree. This object is the **Virtual DOM**.\n- **On Update**: The framework generates a *new* VDOM tree and **diffs** it against the previous VDOM tree. The differences are calculated, and a set of optimized \"patches\" are applied to the real DOM.\n\n> [!TIP]\n> Because the VDOM is just a data structure, it's highly flexible. This allows frameworks like React to render to non-browser environments, such as native mobile apps via React Native.\n\n### 3. Fine-Grained Reactivity\n\nThis method eliminates diffing entirely by creating direct relationships between state and the DOM.\n- **Pioneered by**: SolidJS. Now being adopted by Svelte 5 and Vue Vapor.\n- **How it works**: A compiler transforms the template into highly efficient, imperative DOM creation code that runs **once**. Dynamic parts are wrapped in reactive computations (`effects`).\n- **On Update**: State is managed by a reactive system (**signals**). When a signal's value changes, it directly re-executes only the specific effects that subscribe to it. This updates the DOM node without any diffing or traversing a component tree.\n\n> [!IMPORTANT]\n> Fine-grained rendering is extremely performant because it does the minimum work possible. The update cost is proportional to the number of state changes, not the size of the component or template.",
  "transcription": "## Introduction to Declarative Rendering\n\nHi, I'm Ryan Carneato, the creator of the JavaScript framework SolidJS. Today I'm going to talk about a topic that really defines JavaScript frameworks.\n\nRendering. That is, rendering to the DOM. Dozens of frameworks have existed over the years, but surprisingly very little variation on how they work. In fact, I'd say there are three approaches to rendering to the DOM in JavaScript frameworks. Maybe even 2.5, really. I'll let you decide by the end of the video.\n\nToday we're going to look at how that rendering works. If you understand how rendering works in declarative JavaScript frameworks, you basically understand the framework. So, what do I mean by declarative? Well, I mean with templating where it inputs state and outputs UI. To understand what I'm talking about, I think we should probably look at the anatomy of a template in a JavaScript framework.\n\n## The Anatomy of a UI Template\n\nSo, I've got a JSX type template here. But really, all templates kind of work the same. The idea here is that if you look at it, there are static parts like the section and H2 content, things that can basically never change. And then there's the dynamic parts that I'm calling holes, like this title or description, or really this whole list map function here.\n\nNow, different rendering approaches treat these things different ways, but it's important to kind of see your template in these terms to understand where the holes are. Because like, even inside holes, you can have sub-templates like this list item, and inside it, more holes. So templates really are the kind of composing of several different templates until they render the final output.\n\n## The Inefficient `innerHTML` Method\n\nSo how do we implement this? We can start with the way they don't work today, or at least anymore, which is what Backbone did or maybe what you used to do with jQuery. That would be take our template and combine it into a string and `innerHTML` it. In fact, I put together a simple example to show exactly how that works. I will be using this example throughout this video.\n\nBasically, it's a simple counter where on interval I am setting the state, which will update the contents of a div. I'm using interval so we don't really have to get too much into state management and we can just focus on the rendering. In this basic example though, we are just returning a string from our template function which is just a div and then the latest count. This `setState` is basically the trick to us knowing when we need to trigger that template again. And in fact, in our base class, that's what I've done. When we initially create the app, there's an internal template function we call, this `_t`, which then applies the template and `innerHTML`s it. And when we call `setState`, same thing happens again. So new state update, rerender our string, `innerHTML` it. This works, as you can see.\n\nUnfortunately, it's fairly unperformant. It, in fact, unmounts the whole div inside. That's why the even the ID `app` div is flickering here. Basically the bigger your template gets, the more DOM elements you're creating with every update. And that is not efficient. No modern JavaScript framework could work this way.\n\n## The Problem with Direct DOM Diffing\n\nSo how do we avoid recreating the DOM every time? Well, we can diff against it: check if an element has a value, compare it to our new value, and see if it's changed, and if so, we can then update it. But it turns out that that can actually be slow, too.\n\nFor example, `childNodes`. This is a property on a DOM node which lets us iterate through the children. It's an array, but in the underlying engine, it's actually a linked list. So if you use that property, you actually realize the whole array instead of walking through it one at a time like the underlying engine does. This is actually expensive for memory and bad for performance. There's actually a number of properties on the DOM kind of like that, things that force paint or layout calculations when you read them. And this makes it kind of a landmine. So we don't really always want to read from the DOM either. This is why people say the DOM is slow. It isn't innately, because well, every JavaScript framework has to use the DOM to update the UI. But it's very easy to deoptimize. So if we don't want to recreate the DOM every time and we don't want to read from it, what can we do?\n\n## Method 1: Dirty Checking (Diffing the Holes)\n\nSo that brings us to our first modern method for rendering. I'm going to call it dirty checking. It was pioneered by Angular but used by Ember and Svelte and Lit and basically any non-virtual DOM library historically.\n\nAgain, our example looks pretty similar. We initialize some state, we set that state on an interval, and we pass it to our template, except this time it's a tag template literal. I'm using this approach because Lit is a very modern approach to this, but we've used different tools over time to basically accomplish the same thing inside. You'll notice one key difference. We only append now in the constructor. Now, when we set state, we just call our template function and it handles everything else.\n\nBut the template function is not the same each time. This `html` function, we actually do something different on creation than from update. In the case of Svelte, they actually basically pre-compile the work that needs to be done on creation and then only worry about the updates. But with something like Lit, which is basically what I'm doing here, it will run that on the very first time a template is created. And it works because for Lit, tag template literals have this cool property where the static nodes, basically like the string on the outside, the div here, get cached with the same reference. Which means that we have this amazing ability to put it in a map and do lookups, and this way we can associate the bindings.\n\nWhat are bindings, you're asking? Bindings are basically our data format that I've created here to describe the holes, because our approach here will be based on diffing the holes. We don't care about the static parts, they never change, but the holes can change. So we first process our template to kind of extract out where the holes are and then each update, we just update the holes by comparing them. So here what I've done is I've actually taken our template string here and I've actually basically joined it together with comment nodes to fill in the holes and instantiated our template into DOM elements. This is how we render it the first time. We actually use those DOM elements and crawl through them with this `createBinding` call right here to actually find those comments. And I'm going to use a tree walker to kind of show this off. And depending on the context of where we find it, we might push on different types of bindings. In our case, I kept it simple. This is again overly simplified. There'd be different types of checks here, but in our case, we just, we have an insert type that I've defined, which references that comment node that we put in and has a value of undefined initially. It won't have a value of undefined for long, but this lets us initiate the bindings. So now the first time we run our template literal function, we do all this processing, figure out where all the bindings are in the DOM, create our template, and then every subsequent time we do this, we just call the diff function of the holes that come in against the previous bindings.\n\nSo how does this diff function work? Well, again, this is oversimplified. I've only created the code to support one binding type, which is insert, but you kind of get the idea. It goes through and then if it finds a binding that's undefined, it'll go, okay, well, we need to get the reference node, which in this case is the comment node, and insert a new text node before it with the new with the value from the holes. If the value already exists, then we can simply just take that previous element and update its node value with the new value. So as long as every time we call our `setState` up here, we rerun our template, it will automatically diff the differences of the holes and keep the static stuff the same. And this is, as I said, how Angular works, Svelte, Lit, Ember, a whole number of non-virtual DOM frameworks.\n\n## Method 1.5: The Virtual DOM\n\nBut I mentioned a moment ago that there are 2.5 ways to render. Let's introduce that 0.5 right now. Pioneered by React, used by Vue, Inferno, Preact, Elm, Mithril. Uh, honestly, there's so many more. The virtual DOM is probably the most popular way frameworks render. And I'm going to show you that it's basically the same as dirty checking. There are some key differences, though. So, let's take a quick look here. This time you'll notice that instead of a tag template literal, we have a hyperscript function, or some frameworks call this the `createElement` call. And you'll notice right away that the div is just a string. Now we no longer have static parts. The whole tree is being recreated by this function call. But other than that, this is largely the same. We're calling our `setState` which updates our count and in our framework implementation, what you're going to see is when we do that, we recall our template and we diff it. In fact, the probably most interesting difference here is that the diffing and the template code is basically the same from an external standpoint, both on initialization and setting the state. It does do different operations when things are new versus when they already exist, but the process is the same regardless of if it's the first time or it's any subsequent time.\n\nSo, first of all, what does our H function do? Well, in this case, I implemented a simple one that just goes through the list of arguments and essentially constructs our virtual DOM node. In fact, let's just console log it to see what it looks like. Console log our node. And you can see it here. It has attributes, children, a type of div, and actually if you go to children, there's a text node in here with a value of zero. And if I go further down, you're going to see that same text node. Well, not the same because it's a different rendering, but same place in the tree with a value of nine. So, we just recreate this tree on every render that's being triggered by that set interval.\n\nSo, from there when we diff, well we can go, is it, does it not exist? Is it a new node? Has the type changed? And if so, you know, remove the old node and create the appropriate types of node. Is it a text node? Is it an element? And append them. We can do some special handling here with the text node, you know, setting the text content. We can diff the attributes, setting the attributes if they've changed, and diff the children. This is basically how we propagate through our whole tree.\n\nAll in all, this is still dirty checking. It's still a diff, except it happens in two passes instead of one. We're not just diffing the holes this time, we're actually constructing a structure and then diffing that completely. Now, that does allow for some dynamicism. It also allows the virtual DOM to very easily go in other environments, let's say like mobile where there isn't a DOM, because it isn't until you get to the diff down here that you actually are dealing with DOM nodes or with things specific to the browser. The virtual DOM part here is simply just a data structure. So it gives virtual DOMs a lot of flexibility. And importantly, when we look here, you can see that again, we're just updating the one text node in here. We're not redoing the whole div. So even though we are recreating these virtual nodes, we're not recreating the DOM nodes.\n\n## Method 2: Fine-Grained Reactivity\n\nSo I mentioned earlier that there was one other approach and it's pretty different from the others. Popularized by Solid, we've started to see this approach getting picked up by Svelte 5 and the upcoming Vue Vapor. And that's fine-grained rendering.\n\nFor the fine-grained rendering example, I see no point keeping it a class because there's no state that belongs to the component, so to speak. There's no need for a `setState`. We have our own change management system. In fact, the `createSignal` and `createEffect` are the only thing being imported from the quote framework. In fact, there's kind of no framework here.\n\nInstead, our template gets compiled to this. Now, this does not look like a template anymore. The Hyperscript functions still kind of look like a template. So did the tag template literal. But the truth is pretty much all JavaScript frameworks use a compiler these days. So whether your JSX gets compiled to this or your JSX gets compiled to this, it's not really that big of a difference. Templates generally do get compiled for performance reasons, but otherwise this example is very similar. We create some state and we set an interval that updates that state every second. Return an element and then we call our function once and we attach it to the DOM. So what's the framework doing? Well, I made again a very oversimplified version of this, but essentially we have a signal which has observers. And when you read from a signal, if there's an observer, and this observer is set in the global scope, I'll show you how that works in a second. We add it to the signal's observers. Then later when we set the new value, we iterate through the list of observers and call each one of them to basically let them know things have changed. In our case, our observer is an effect, which is just a function here. And we call it initially when it's created, but then after the fact whenever it's called, when the value updates, it simply sets itself as the observer, calls its internal function, which will then read from some signals, including our count in this case, which will add it to its list. And when we're done, we just remove ourselves from that global scope. And that's really all there is to it, because it just closes the loop and you get an update.\n\nSo in a sense, this approach is very, very simple, yet very, very efficient. Again, it only updates the one text node, unsurprisingly, because that's the only code that runs. Instead of diffing a virtual DOM or diffing the holes, it just runs once and then just updates what has actually changed.\n\n## Summary and Trade-Offs\n\nAnd that's basically rendering in JavaScript frameworks in a nutshell. There are trade-offs with each approach, to be fair. Obviously the virtual DOM gives a little bit more dynamicism in some cases. You know, some approaches are more performant or require you to opt into things like say, signals as your change management system. But in the end, there are really only these few ways that all JavaScript frameworks work. If you enjoyed the content of this video and you want to see more beginner content like this, let me know in the comments. You know, like and subscribe and I'll keep on making."
}
