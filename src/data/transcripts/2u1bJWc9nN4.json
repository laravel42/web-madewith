{
  "schemaVersion": 3,
  "videoId": "2u1bJWc9nN4",
  "language": "en",
  "source": "auto",
  "seoDescription": "Learn to build a robust Access Control List (ACL) in Laravel by creating custom middleware to protect routes based on user roles. This tutorial covers extending the User model with `hasAnyRole()` and `hasRole()` methods for efficient permission checks, using Blade directives to dynamically display user roles in the UI, and generating a `CheckRole` middleware with Artisan. You'll see how to retrieve role requirements from route definitions and implement the core logic to grant or deny access, culminating in registering the middleware.",
  "chapters": [
    {
      "title": "Enhancing the User Model for Role Checks",
      "description": "This chapter covers adding the `hasAnyRole()` and `hasRole()` methods to the User model to enable role-checking logic.",
      "startTime": 0.0,
      "endTime": 308.0,
      "slug": "enhancing-the-user-model-for-role-checks"
    },
    {
      "title": "Displaying User Roles in the Admin View",
      "description": "This chapter shows how to modify the admin Blade view to display checkboxes indicating a user's assigned roles by using the new `hasRole()` method.",
      "startTime": 308.0,
      "endTime": 443.0,
      "slug": "displaying-user-roles-in-the-admin-view"
    },
    {
      "title": "Creating the Role-Checking Middleware",
      "description": "This chapter demonstrates how to generate a new middleware file using an Artisan command and begin implementing the `handle` method to check for authentication.",
      "startTime": 443.0,
      "endTime": 607.0,
      "slug": "creating-the-role-checking-middleware"
    },
    {
      "title": "Implementing Core Middleware Logic",
      "description": "This chapter explains how to retrieve role requirements from route actions and use the `hasAnyRole()` method to decide if a user can proceed.",
      "startTime": 607.0,
      "endTime": 753.0,
      "slug": "implementing-core-middleware-logic"
    },
    {
      "title": "Registering the Middleware and Next Steps",
      "description": "This chapter concludes by showing how to register the new middleware in `Kernel.php` with a route alias and outlines the next steps.",
      "startTime": 753.0,
      "endTime": 810.56,
      "slug": "registering-the-middleware-and-next-steps"
    }
  ],
  "summary": "## User Model Enhancements\n\nTwo crucial methods are added to the `app/User.php` model to handle permission checking:\n\n*   **`public function hasAnyRole($roles)`**: Checks if a user has at least one of the specified roles. It is designed to handle both a single role passed as a string and multiple roles passed as an array.\n*   **`public function hasRole($role)`**: A helper method that checks if the user is assigned a single, specific role. It queries the `roles` relationship to see if a role with the given name exists for that user.\n\n## Displaying Roles in Blade Views\n\nTo visually confirm user roles in the admin panel, the `admin.blade.php` file is updated. Checkboxes are used to represent each role, and their `checked` status is determined dynamically.\n\n```html\n<input \n  type=\"checkbox\" \n  {{ $user->hasRole('user') ? 'checked' : '' }}\n  name=\"role_user\">\n```\n\n## Creating the Role-Checking Middleware\n\n> [!TIP]\nA new middleware is generated using the Artisan command: `php artisan make:middleware CheckRole`\n\nThe middleware file is created at `app/Http/Middleware/CheckRole.php`.\n\n## Middleware Logic (`CheckRole.php`)\n\nThe core logic resides within the `handle($request, Closure $next)` method.\n\n1.  **Authentication Check**: The middleware first verifies that a user is logged in. If `$request->user()` is null, it immediately returns a `401 Unauthorized` response.\n    \n2.  **Retrieving Role Requirements**: The middleware inspects the current route definition to find which roles are allowed.\n    \n    > [!NOTE]\n    > Role requirements are defined as a custom `roles` key in a route's action array. The `$request->route()->getAction()` method retrieves this entire array, from which we can extract our custom `'roles'` value.\n\n3.  **Permission Check**: The final decision logic allows the request to proceed if either of two conditions is met:\n    *   The user has one of the roles specified for the route (`$request->user()->hasAnyRole($roles)`).\n    *   No roles are specified for the route (`!$roles`).\n    \n    > [!IMPORTANT]\n    > Allowing access when no roles are defined ensures that the middleware doesn't block routes that are intended to be public or are not part of the ACL system.\n\n4.  **Access Denied**: If neither condition is met, the user lacks permission, and the middleware returns a `response('Insufficient permissions', 401)`.\n\n## Registering the Middleware\n\nTo make the middleware usable in route files, it must be registered with an alias in `app/Http/Kernel.php` inside the `$routeMiddleware` property.\n\n```php\nprotected $routeMiddleware = [\n    // ... other middleware\n    'roles' => \\App\\Http\\Middleware\\CheckRole::class,\n];\n```\n\nThis allows you to protect routes using the alias, like `->middleware('roles')`.",
  "transcription": "## Enhancing the User Model for Role Checks\n\nWelcome back. In the last part, we set up our database. We created the roles and the users there and our first connections, or we assigned the first roles. Now it's time to do the remaining work to create the actual middleware which protects our route. I start in the user model.\n\nHere, I will define two new methods. This is one of the rare times where we actually define more than just the relationships in the user model, but here I really need these extra methods. The first method I will need is a public function which is called `hasAnyRole`. With this method, I want to have a way to check if a user, later in the middleware, the authenticated user has a certain role. I need this method to find out if he has this role and to be able to determine if the user should be granted access to a certain resource or not. I will pass an argument called `roles` which will contain the roles I want to check for. So this will work in a way that when we're in the middleware and we know that a certain route is only accessible by let's say 'admin' and 'author', we would pass 'admin' and 'author' to this function and check if the user has one of the two roles. And only if he has one of those roles, he's allowed to access this resource.\n\nIn this function here, I will first check if `roles` is an actual array because we are also allowing to pass a single role instead of an array of roles. Like convenience here, you could of course also pass a single role in an array, but we're providing this convenience here. So if we're in an array of roles, then I want to loop through this array, through all the roles in this area with a normal `foreach` loop. And I will check if this, the current user—this of course refers to the currently logged in user or on whatever user object we're calling this `hasAnyRole` later—if this user has a role. This function doesn't exist yet, I'm going to create it in a minute. If he has this role, not roles, role, if he has this role, I will return `true` because then I want to return with the method that the user actually has this role, which is exactly the check I'm running here. And if I'm checking if he has any of these roles, I don't need to continue in this loop because I know, yeah, he has any of the roles he needs to access this resource, so we may just finish. It doesn't matter if he has all the roles.\n\nThis is the one part of this function. The other is if we're not getting an array of roles but a single role, well then of course the only thing we do is basically repeat this, but here we have `role` because even though it's called `roles`, it's only one role as we're not having an array. So then we would also return `true`. In all other cases though, we would return `false` because the user does not have the role or any of the roles we're checking.\n\nThis leads to the second function, the `hasRole` function, which we're calling here. And this `hasRole` function will check if the user has a specific role. I do this by running or checking if `this->roles()`, which is the relation to all the roles—again using parentheses to stay in the query builder so to say—to say, okay, I'm going to add some arguments to this database call I'm making here. And here where the name of the role is `role`, and then the first element.\n\nNow, what this does is it accesses the roles of this user and then sees if in these roles the user has assigned to it, the role we're checking for appears. If this is not the case, then the user does not have this role. So here I can return `true`, otherwise we're returning `false`. So this is how we prepare our user model to be able to check if the user has a certain role. And with this, we're well prepared to write our actual middleware.\n\n## Displaying User Roles in the Admin View\n\nBefore I do this, I want to do one other thing in my admin blade view. Here I want to populate this admin view page so that we can actually see the check marks here, because we've got all the functionality we need to show the check marks. To do this, I'm going to create an input element here which is of type checkbox. And if it is checked or not depends on the case if this user, the current row we're in, has this role or not. So here I'm entering the Blade template expression, and I check if the current user—we're looping through all users here to output them in the table—if the current user has a role, and in this case, well, it's just a user role. I'm doing this hardcoded here. Of course you could come up with a dynamic way, but this is already split up over several videos and I didn't want to make this too long, and it's not a core thing to this ACL system I'm showing. So this is how we check if a user has a role. This is just the function we implemented a few minutes ago. And if he has this role, well then I'm going to set the `checked` attribute, otherwise I'll not.\n\nNow we'll just copy this for all three roles. So here I have the `author` and here I have the `admin`. And I also want to set a name attribute here. This is needed later when we're actually able to change the roles. This will be called `role_user`. This input here, this input will be called `role_author`, and the last input will of course be called `role_admin`. Now with these changes in place, if I reload this page, you can see the actual role assignments we have due to our seeding. And this looks all right to me. Author has the author role, admin the admin role, and our normal user the user role. So that's what works.\n\n## Creating the Role-Checking Middleware\n\nNow back to the middleware. We're going to create a new middleware. And here again, we can use our Artisan to help us set up this file. `php artisan make:middleware` and then the name of the middleware, which I will just call `CheckRole`.\n\nNow in my middleware folder under app/Http, I have this `CheckRole.php` file, which is conveniently already set up in a way that we don't have to do that much. Inside this `handle` function, I first want to check if we actually have a user in our request. So if this is not null. This means if we're trying to access this not logged in, then we will always be sent back on all the routes where we are checking for roles, because no matter which role you theoretically have, if you're not logged in, you're certainly in the wrong place. So then I will return a `response`, and this will just be a text response where I say \"Insufficient permissions\" with error code 401 Unauthorized. I have to do this because if I did not do this, I'll try to access this user of the request in the next steps, and this would throw an error if the user is not set. Therefore, I will need this check before I actually do this.\n\nNext thing is I need to retrieve some actions. I will come back to this in a second. I do this by accessing my route, the route we're currently trying to access here when this middleware kicks in, and here the `getAction` method. Now this is a little bit difficult to understand, I imagine, and it will be clearer in a few seconds. Basically with `getAction` we can access these key-value pairs we're setting up in our routes array. Here, all these are actions of this route, and we can define our own actions, and we will do so later. So these actions we're manually defining then will be the actual roles which are allowed to access a route. And in this CheckRole middleware, I'm basically retrieving these roles here by getting the actions.\n\n## Implementing Core Middleware Logic\n\nHowever, actions is this array. This is the actions variable, or this is what we store in this action variable. Later we'll have like `roles` and then an array which says `author`, `admin` for example. And then we want to retrieve this `roles` key in our actions. To do this, back in my `CheckRole`, I will add a new variable which basically first will check if `actions` does have this `roles` key, because not all our routes need it. We're also having routes which are not protected, which may be accessed by everyone where we don't use this role middleware or where we set up this role middleware but don't define any roles. So first we check if we actually do have any roles set up, and if this is the case, well then all we do is we store them. The roles we set up for this route are stored in this `roles` variable. Otherwise, the variable will be `null`.\n\nSo next step is to access the user in the request, and this again is the reason why we have to check if the user is null before we do this here. And use the `hasAnyRole` method we created earlier in this video to check if he has any of the roles we retrieved here, or if `roles` is not set. If `roles` is not set, well then also we want to allow the user to proceed. And we do this by calling `return next($request)`, the statement that was at the end of this default `handle` method created for us by the Artisan command. This basically says everything's fine, you may go on with the next middleware, whatever. So we're done, we're done checking, everything's fine, you may continue. And you may continue if you either have one of the roles or if no roles are set up. Otherwise, you're trying to access a resource you're not allowed to and we will—let's just copy this—we will return the exact same response.\n\n## Registering the Middleware and Next Steps\n\nThis is how you create the middleware. Now the last step in this video is to register this middleware. And we do this in the `Kernel.php` file at the very bottom of this file in the `routeMiddleware` area. Here at the end, I will create a new middleware and I will just call it `roles`. The name is up to you. And this will point to `App\\Http\\Middleware\\CheckRole`. And here it's important to add this `class` keyword or access here property so that we refer the actual class.\n\nWith this, the middleware is created, it is registered, and in the next step, in the next video, we will actually protect some routes with it and do all the final touches we have still got to do. See you there. Bye."
}
