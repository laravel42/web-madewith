{
  "schemaVersion": 3,
  "videoId": "v6Nr7Zman_Y",
  "language": "en",
  "source": "auto",
  "seoDescription": "Clear, practical explanation of ASP.NET Core dependency injection lifetimes—AddSingleton, AddScoped, and AddTransient—using an IEmployeeRepository example. Demonstrates interface and implementation, service registration in Startup.ConfigureServices, and the runtime behaviors across HTTP requests and within request scopes. Includes concrete controller and view injection examples, differences in instance reuse, and when each lifetime is appropriate for in-memory or shared state.",
  "chapters": [
    {
      "title": "Introduction and goal",
      "description": "Introduces the video topic: differences between AddSingleton, AddScoped, and AddTransient in ASP.NET Core using an example application.",
      "startTime": 0.0,
      "endTime": 18.0,
      "slug": "introduction-and-goal"
    },
    {
      "title": "IEmployeeRepository interface",
      "description": "Shows the IEmployeeRepository interface and explains the GetAllEmployees and Add methods used in the example.",
      "startTime": 18.0,
      "endTime": 56.0,
      "slug": "iemployeerepository-interface"
    },
    {
      "title": "MockEmployeeRepository implementation",
      "description": "Shows the MockEmployeeRepository class implementation, how it stores an in-memory list, and how the constructor populates defaults.",
      "startTime": 56.0,
      "endTime": 120.0,
      "slug": "mockemployeerepository-implementation"
    },
    {
      "title": "Registering services in Startup.ConfigureServices",
      "description": "Examines Startup.cs ConfigureServices, shows how the interface is tied to the implementation via AddSingleton, and mentions alternative AddScoped and AddTransient.",
      "startTime": 120.0,
      "endTime": 180.0,
      "slug": "registering-services-in-startup-configureservices"
    },
    {
      "title": "Controller and view dependencies; Create flow",
      "description": "Describes HomeController dependency injection, the Create action for HTTP POST, the create form, and plan to show total employees count in the view.",
      "startTime": 180.0,
      "endTime": 300.0,
      "slug": "controller-and-view-dependencies-create-flow"
    },
    {
      "title": "Injecting repository into view and computing count",
      "description": "Explains using @inject directive to inject IEmployeeRepository into the view and using GetAllEmployees().Count to show the total.",
      "startTime": 300.0,
      "endTime": 420.0,
      "slug": "injecting-repository-into-view-and-computing-count"
    },
    {
      "title": "Singleton behavior demonstration",
      "description": "Demonstrates AddSingleton: one instance is created and reused across requests, so added employees persist across HTTP requests in this in-memory repository.",
      "startTime": 420.0,
      "endTime": 620.0,
      "slug": "singleton-behavior-demonstration"
    },
    {
      "title": "Scoped service behavior",
      "description": "Switches to AddScoped and explains scoped semantics: same instance within a single HTTP request, new instance across requests, demonstrated with the Create GET/POST flow.",
      "startTime": 620.0,
      "endTime": 820.0,
      "slug": "scoped-service-behavior"
    },
    {
      "title": "Transient service behavior",
      "description": "Switches to AddTransient and demonstrates that transient provides a new instance every time it's requested, even within the same HTTP request, so controller and view get separate instances.",
      "startTime": 820.0,
      "endTime": 880.0,
      "slug": "transient-service-behavior"
    },
    {
      "title": "Summary and close",
      "description": "Wraps up with a concise recap and points to a summary table comparing scoped, transient, and singleton lifetimes.",
      "startTime": 880.0,
      "endTime": 923.0,
      "slug": "summary-and-close"
    }
  ],
  "summary": "## Key Concepts\n\n- **Dependency Injection lifetimes**: AddSingleton, AddScoped, AddTransient control when service instances are created and reused.\n\n## AddSingleton\n\n- One instance for the entire application lifetime.\n- Created the first time the service is requested and reused across all HTTP requests and components.\n- Changes to in-memory state (like the MockEmployeeRepository _employeeList) persist across requests.\n\n> [!IMPORTANT]\n> Singleton is suitable for shared, thread-safe state or stateless services. Avoid storing per-request or mutable per-user state.\n\n## AddScoped\n\n- One instance per HTTP request scope.\n- Same instance reused within a single request (controller action and view rendering), but a new instance for each new request.\n- In the example, a POST that adds an employee and then re-renders the view sees the added employee because both actions are in the same request scope.\n\n> [!TIP]\n> Use Scoped for services that should be consistent during a single request (e.g., unit-of-work, per-request caches).\n\n## AddTransient\n\n- A new instance every time the service is requested, even multiple times within the same HTTP request.\n- Controller and view get separate instances if both request the service, so in-memory changes made by one are not visible to the other.\n\n> [!NOTE]\n> Transient is good for lightweight, stateless services or short-lived operations.\n\n## Example takeaways\n\n- The MockEmployeeRepository seeded three employees in its constructor; observing counts illustrated lifetime differences.\n- Singleton: count increases cumulatively across requests.\n- Scoped: count reflects additions only within the same request; repeated requests show fresh initial state plus the one added during that request.\n- Transient: controller adds to its instance, but the view requests a separate instance, so the displayed count remains the initial value.\n\n## Final advice\n\n- Choose the lifetime based on desired instance sharing and thread-safety.\n- Avoid storing mutable request-specific data in singletons.\n- Prefer scoped for request-scoped data and transient for lightweight, stateless services.",
  "transcription": "## Introduction and goal\n\nThis is part 44 of ASP.NET Core tutorial. In this video we'll discuss the difference between AddSingleton, AddScoped, and AddTransient methods in ASP.NET Core with an example.\n\n## IEmployeeRepository interface\n\nThis is the same application that we've been working with so far in this video series. Here is our IEmployeeRepository. In this example we'll only be using these two methods: GetAllEmployees and Add. As the name implies, the GetAllEmployees method returns us the list of all employees and this Add method adds the new employee that we provide to this method as an argument to our repository. And this is just the interface.\n\n## MockEmployeeRepository implementation\n\nHere is the class that implements our interface IEmployeeRepository. Here's the implementation for Add method. As you can see, the new employee object that we are providing as an argument to this method, it is adding that employee to the private field _employeeList, which is obviously a list of employee objects. And then if we look at GetAllEmployees method, this method is returning us the list of all employees that we have again in this private field _employeeList. The important point to keep in mind is this is an in-memory list, meaning every time we restart our application we'll lose the changes that we make to this private field _employeeList. Whenever we create an instance of this class MockEmployeeRepository obviously its constructor is called, and as you can see within the constructor we are initializing this private field _employeeList with three employee objects to start with.\n\n## Registering services in Startup.ConfigureServices\n\nHere is our Startup.cs file and within this file we have a Startup class and within the class ConfigureServices method. As the name implies this is a function that we use to configure services required for our application. At the moment we have this interface IEmployeeRepository and this MockEmployeeRepository class provides the implementation for that interface. So to tie these two together we are using AddSingleton method of IServiceCollection. Basically by including this line here we are saying if someone asks for IEmployeeRepository then provide them an instance of the MockEmployeeRepository class. In addition to AddSingleton method we also have these two methods AddScoped and AddTransient. For now let's use this AddSingleton method and see the behavior that we get.\n\n## Controller and view dependencies; Create flow\n\nHere is a HomeController. Notice the HomeController has a dependency on IEmployeeRepository. We are injecting IEmployeeRepository into the HomeController using its constructor. We are then using this injected dependency within the Create action method. This is the Create action method that responds to the HTTP POST request. Here's our create employee form. After we provide all the details of the new employee and then when we click this Create button this form is posted to the server. This HTTP POST request is handled by this Create action method. The new employee that we want to create is passed as a parameter to this Create action method which we are then passing to the Add method of EmployeeRepository. This method adds it to the employee repository. After we have successfully added the employee object to the repository we are then redirecting the user to the Details action method so we could see the details of that newly added employee. For the time being let's not redirect the user to the Details action. After we successfully add the employee we want to stay on the Create view itself, so let's comment this line which redirects us to the Details action method. Next on our Create view we want to display the total number of employees that we have in our employee repository. For that let's make a copy of this view and then change the bits that are required. We want to display the total employees count right here. We don't need another Create button so let's get rid of that first. We want to include this little text: Total employees count equals. Now there are several approaches to compute the total number of employees that we have in our employee repository. The approach that I am going to take is inject employee repository service into this view as well.\n\n## Injecting repository into view and computing count\n\nNow remember to inject the employee repository service into a HomeController we are using the constructor. To be able to inject a service into a view we use the @inject directive. In our case the service that we want to inject is IEmployeeRepository. Let's call the variable _empRepository. We can now use the service instance to compute the total number of employees. So at this location we want to switch to C# mode so we use @. On this service we have GetAllEmployees method which is going to return us the list of all employees and on this we use the Count method we get the total number of employees.\n\n## Singleton behavior demonstration\n\nHere's the important bit to understand. After we provide the details of the new employee that we want to create and issue a POST request by clicking this Create button to handle this POST request our application needs an instance of IEmployeeRepository in two places: first within our HomeController in this Create action method that responds to the POST request so to be able to add the new employee to our employee repository we need an instance of IEmployeeRepository service. Next we also need an instance of the service in our Create view. Why do we need it in the Create view? To be able to compute the total number of employees that we have in the employee repository. At the moment we have registered our service using AddSingleton method. So with all these changes in place let's see the behavior that we get. Notice when we issue a GET request to /Home/Create we see the total employees count is 3. Let's understand what's happening behind the scenes. To serve this request an instance of HomeController is created and if we take a look at our HomeController notice it has a dependency on IEmployeeRepository. This is the first time an instance of IEmployeeRepository is requested, so ASP.NET Core creates an instance of the class that implements this interface as you can see in this ConfigureServices method. In our case an instance of MockEmployeeRepository class is created and injected into the HomeController. We have issued a GET request to the Create action of our HomeController. So within our HomeController this Create action method that handles the incoming HTTP GET request all we are doing within this action method is executing return View statement. Since the name of the action method is Create this Create view is then executed. If we scroll all the way to the top you can see this view also has a dependency on IEmployeeRepository. Since we are using AddSingleton method to register a service, instead of creating another instance of this MockEmployeeRepository class for the Create view it is going to reuse the instance that ASP.NET Core has created when this HomeController has first requested an instance of IEmployeeRepository. So that same instance will also be injected into the Create view. The view then uses the service instance to calculate the total number of employees. If you recollect when we create an instance of the service class within the constructor we are populating this private field _employeeList with three employee objects, so the total count of employees that we see here is 3. Now let's add a new employee and see what happens to the total count. Notice when I click the Create button the total employees count is 4. If I click it again it increases to 5. One more time 6. It goes on. So every time we click this button the employee count increases by 1. The important point to keep in mind is every time we click this Create button we are issuing a new HTTP request. Since we are using AddSingleton method to register a service there's only one instance of the service. An instance is created when the service is first requested and that instance is then reused throughout our application across all the HTTP requests. That is the reason every time we click this Create button the total employees count increases by one. With a singleton service there's only a single instance; an instance is created when the service is first requested and that single instance is used by all HTTP requests throughout the application.\n\n## Scoped service behavior\n\nNow let's use AddScoped method instead of AddSingleton method to register a service and see the behavior that we get. With a scoped service we get the same instance within the scope of a given HTTP request but a new instance across different HTTP requests. Let's understand what this means with an example. We made a change to our application code so when we issue a GET request the application restarts and we should see the count of employees as 3. There we go. Now let's provide the details of a new employee and click the Create button. Notice the employees count increased to 4. From this point no matter how many times we click this Create button the total employees count stays at 4. Let's understand why we are getting this behavior. When we issued a GET request to /Home/Create we get a new service instance because remember with a scoped service every new HTTP request gets a new service instance. With a new service instance we are creating three employee objects, so when we issued a GET request to /Home/Create we saw the total employees count as 3. We then provided the details of a new employee and then issued a POST request by clicking the Create button. When we issue a POST request it is another new request, so we get another new instance of the service. With a new instance of the service by default we have a total of three employees, but then the Create action within our HomeController is adding another new employee, so that total count increases to 4. Our Create view can see that increased count because after adding the employee to the employee repository we are re-rendering the Create view. If we take a look at the Create view, the Create view also has a dependency on IEmployeeRepository service, but here a new instance of the service will not be provided; the instance that we have used to add the employee to the employee repository, that same instance is reused in the Create view. Why is that? That's because both these actions, that is adding the employee to the employee repository and rendering this Create view, are happening as part of the same HTTP request. They are happening in the same scope of that HTTP request. Remember with a scoped service we get the same instance within the scope of a given HTTP request but a new instance across different HTTP requests. So when this Create button is clicked, adding this new employee to the employee repository and re-rendering this Create view both these actions are happening in the scope of this same HTTP POST request, so we see the total employees count increased to 4. After the value is increased to 4 no matter how many times we click this Create button the count does not go beyond 4. Why is that? Well that's because every time we click this Create button we issue a new HTTP request, and remember with a scoped service with every new HTTP request we get a new service instance and with a new service instance the default initial count of employees is 3. When we click the Create button we add the fourth employee so we see the total employees count as 4. Next time I click the Create button we get another new service instance and with that new service instance again the initial count of employees is 3 where a new employee is added and the count becomes 4, and that's what we see in the browser. So every time we click the Create button the same process repeats and hence the count doesn't go beyond 4 because with every request a new service instance is created with a scoped service.\n\n## Transient service behavior\n\nNow let's use AddTransient method instead of AddScoped method to register a service and see the behavior that we get. With a transient service a new instance is provided every time an instance is requested whether it is in the scope of the same HTTP request or across different HTTP requests. Notice when we issue a GET request to /Home/Create we see three as the total count of employees. Now let's provide the details of a new employee and then click the Create button. Notice no matter how many times I click the Create button the total employees count does not move beyond three. Let's understand why this is happening. Remember with a transient service a new instance is provided every time an instance is requested whether it is in the scope of the same HTTP request or across different HTTP requests. So if we relate this to our example notice our HomeController is asking for an instance of IEmployeeRepository. For that transient service we get a brand-new instance. So with a new instance of a service the initial default count of employees is 3 and then the Create action within our HomeController is adding a new employee to the employee repository so that count becomes 4 and then it re-renders the Create view. If we take a look at the Create view notice the Create view is also asking for an instance of IEmployeeRepository. Remember again with a transient service every time we ask an instance we get a brand new instance, so another new instance of the service is created and this new instance again the initial count is 3 and that is what we see on the web page. So the point that I'm trying to make is HomeController has its own instance of the service instance and the Create view has its own instance of the service instance. This obviously means that change made by the HomeController to its service instance cannot be seen by the Create view and the vice versa is also true.\n\n## Summary and close\n\nThe table on this slide summarizes the difference between a scoped service, transient service, and a singleton service. That's it in this video. Thank you for listening."
}
