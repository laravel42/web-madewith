{
  "schemaVersion": 3,
  "videoId": "aYTjfp4K8RE",
  "language": "en",
  "source": "auto",
  "seoDescription": "Learn the fundamentals of October CMS plugin development by creating a simple, custom component from scratch. This tutorial walks you through setting up the necessary directory structure, writing the core PHP logic for your component class, and registering it within your plugin. Discover how to fetch and prepare data using the `onRun` method and render it on your website's front-end with a default Twig partial.",
  "chapters": [
    {
      "title": "What Are October CMS Components?",
      "description": "This chapter introduces the video topic and provides a refresher on what components are by demonstrating the pre-existing blog post list component.",
      "startTime": 0.0,
      "endTime": 92.0,
      "slug": "what-are-october-cms-components"
    },
    {
      "title": "Component File Structure",
      "description": "This chapter explains how to create the required directory and file structure for a new custom \"actors\" component.",
      "startTime": 92.0,
      "endTime": 170.0,
      "slug": "component-file-structure"
    },
    {
      "title": "Building the Component Class",
      "description": "This chapter covers writing the core PHP class for the component, including namespacing, using the base class, and defining component details.",
      "startTime": 170.0,
      "endTime": 324.0,
      "slug": "building-the-component-class"
    },
    {
      "title": "Preparing Data with the `onRun` Method",
      "description": "This chapter focuses on implementing the `onRun` method to load actor data from the database and make it available to the front-end.",
      "startTime": 324.0,
      "endTime": 391.0,
      "slug": "preparing-data-with-the-onrun-method"
    },
    {
      "title": "Registering and Adding the Component",
      "description": "This chapter shows how to register the new component in the Plugin.php file and then add it to a page via the October CMS backend.",
      "startTime": 391.0,
      "endTime": 519.0,
      "slug": "registering-and-adding-the-component"
    },
    {
      "title": "Rendering Component Data with Twig",
      "description": "This chapter covers writing the Twig markup in the component's default partial to display the list of actors on the page.",
      "startTime": 519.0,
      "endTime": 619.0,
      "slug": "rendering-component-data-with-twig"
    },
    {
      "title": "Review and Next Steps",
      "description": "This chapter reviews the final result, demonstrates the 'fork' feature for component markup in the backend, and previews the next video's topic.",
      "startTime": 619.0,
      "endTime": 693.82,
      "slug": "review-and-next-steps"
    }
  ],
  "summary": "## What are Components?\nComponents are reusable building blocks in October CMS that can be attached to pages or layouts. They encapsulate specific functionality, handle data, and render markup. A component can be as simple as displaying a list of records or as complex as a user authentication form.\n\n## Creating a Custom Component\nCreating a component involves defining its logic in a PHP class and its presentation in a Twig partial.\n\n### File Structure\nFor a component named `actors`, the required structure inside your plugin's directory is:\n- `components/`\n  - `Actors.php`: The main PHP class for the component.\n  - `actors/`\n    - `default.htm`: The default Twig markup partial for the component.\n\n### The Component PHP Class (`Actors.php`)\n- **Namespace**: `YourVendor\\YourPlugin\\Components`\n- **Base Class**: Must extend `\\Cms\\Classes\\ComponentBase`\n- **Data Fetching**: It's good practice to create a separate method (e.g., `loadActors()`) to query the database and fetch data.\n- **Lifecycle Methods**: The `onRun()` method is executed when the component is rendered, making it the ideal place to prepare data for the view.\n\n### The Component Markup (`default.htm`)\n- This is a standard Twig file.\n- Data prepared in the `onRun()` method is available through a variable named after the component's alias (e.g., `actors.actors`).\n- You can use a `set` tag to simplify the variable name within the partial: `{% set actors = __SELF__.actors %}`.\n- Use standard Twig syntax like `for` loops to iterate over data: `{% for actor in actors %}`.\n\n## Key Component Methods & Properties\n\n> [!IMPORTANT]\n> Every component class requires the `componentDetails()` method.\n\n- **`public function componentDetails()`**: Returns an array with the component's `name` and `description`, which are displayed in the October CMS backend.\n- **`public function onRun()`**: Executed when the component runs on a page. Use this method to inject data into the page by assigning values to public properties of the component class.\n- **`public $actors`**: A public property defined in the class that will hold the actors data and make it accessible to the Twig partial.\n\n## Registration and Usage\n\n> [!TIP]\n> Components must be registered in your plugin's main `Plugin.php` file to be recognized by October CMS.\n\n- **`Plugin.php` Registration**: Implement the `registerComponents()` method to return an array mapping the component's class path to a unique alias.\n  ```php\n  public function registerComponents() {\n      return [\n          'Watchlearn\\\\Movies\\\\Components\\\\Actors' => 'actors'\n      ];\n  }\n  ```\n- **Adding to a Page**: In the backend, navigate to a CMS Page, go to the \"Components\" section, and drag your custom component into the page's code editor. This will add the component tag, like `{% component 'actors' %}`.",
  "transcription": "## What Are October CMS Components?\n\nFirst of all, I just want to apologize for any sniveling or coughing during this video because I'm sick at the moment, so that may happen during the recording of this. \n\nIn this video, we are going to take a look at components. As you will see, we will make bare-bones, just a simple component that we can use on our site. And in the next videos, we are going to take that a bit further and add some options to our components.\n\nSo first of all, let's just remind ourselves what components actually are. As you can see, there is nothing much going on on my homepage. But if I go to the backend, go to the home page, and then go to components, I have this list of components that are coming from the blog plugin that I have installed on this site. And as you can see, I have this post list right here. So I'm just going to take that post list, put it right here, and as you can see, I get this box with some options in it. So for example, posts per page and so on. I can turn it off and as you can see down here, we have the call to that component. So `component 'blogPosts'`. It is calling it by this alias right here. \n\nOkay, so if I save the page and go to my homepage now, refresh it, I get new post for his blog post and so on. So this is the post list of that blog plugin. In this episode, we are going to create a similar thing but much, much simpler. We are just going to display the list of actors that we have saved in our October CMS.\n\n## Component File Structure\n\nFirst of all, we have to create directory structure, just like we did for form widgets. And it's going to be `components`, and then I'm going to call my component `actors`. `actors.php`. \n\nNow as you can see, I have this components directory right here, and then I have `actors.php`. We need to create one more file and one more directory, and then in that directory, I'm going to create another file called `default.htm`. \n\nSo it's the similar thing to what we created right here. If we go to actor box partials, and then we have this `widget.htm`. For components, we just create the directory with the same name and call it `default.htm`. Okay, and that's it. Now we have `components/actors` with `default.htm` and then we have `actors.php`. So this is the main file for our component.\n\n## Building the Component Class\n\nOf course, in `plugin.php` we are going to have to register that component. As you can see, we already have this method prepared for us by the Builder plugin. So first of all, we have to create that plugin before we register it. So in `actors.php`, I am going to namespace it. So the namespace is going to be `Watchlearn\\Movies\\Components`. Next, we need to use the `ComponentBase` class. So `use Cms\\Classes\\ComponentBase`. And since we are going to be displaying actors, I'm going to use the Actor model.\n\nOkay, so now that we got that settled, let's just create... extend the `ComponentBase` class. Okay, so now we have to set the details for that function... for that component. So `public function componentDetails`. Okay, and now we return the name and the description of the component.\n\nOkay, that should be it. So the name of our component is going to be Actor List, and the description is going to be a list of actors. Okay, and now we have to set some data for that component. So we want the list of actors. So when we drag our component to our page, we want the list of actors to be displayed. So we are going to create a protected method and call it `loadActors`. And in that method, we are just going to return all of our actors. So the Laravel way, the easiest way to do that is just `return Actor::all()` and that's it.\n\n## Preparing Data with the `onRun` Method\n\nNext thing we need to do, we need to define `onRun` method. So the `onRun` method is the method that comes with October CMS and it's telling the component, well, what to do when that component is displayed. So on run, we just want to load those actors and assign them to a variable.\n\nAnd now we do `this->actors = $this->loadActors()`. So we are calling this `loadActors` method. Okay, and now we just have to define our variable. The variable is going to be a public variable called `actors`. So `public $actors`. And that should be it. Except we have to say `return` right here. Not `return`. And that's it for our component.\n\n## Registering and Adding the Component\n\nNow we have to register that component and create an HTML that will display our data. First of all, I'll just register our component and then try to display it in the backend, so to see if it actually even works, if we didn't screw something up. So we already have this public function method called `registerComponents`, and we just return an array of components that we have. So we only have one component, so we are going to return `Watchlearn\\Movies\\Components\\Actors` and assign it an alias of `actors`. \n\nOkay, that should be it. So `Watchlearn\\Movies\\Components\\Actors`. And now we go to our backend. Let's remove this post list. Okay. Wherever you click inside this text box, that's where the component is going to show up. So let's just go to components right now. Refresh it, first of all. So reload this. And if we go to homepage... I forgot to save the home page. So I have to remove the list again, save it, and now if you go to components, as you can see, we have this Movies menu item right here, and we have Actor List component. So this is our component. I'm just going to drag it right here, actually right here. And now we have this actor list. So as you can see, the only thing we can change is the alias for our actor list. But in the next episode, I'm going to show you how you can put much more options inside of this box and control your component a bit better. So for now, we have this component `actors` right here and we have it here. Save it. And of course, if we go to our homepage right now and refresh it, nothing should actually happen because we still didn't write the HTML for our component.\n\n## Rendering Component Data with Twig\n\nSo we are going to do that right now. So first of all, we have to set the actors variable to be on this page. So to do that, we just open up Twig tags and then just use the `set` function to set the actors. So it's something like setting a variable in PHP. Okay, now we have this `actors` variable set up and now we can use Twig to display those actors' names, for example. So I'm just going to create a `ul` tag. And then inside of that, I'm just going to open up a for-each loop. And inside the for-each loop, I'm just going to display the actor's first name and last name. If we save this, that should actually be it. \n\nLet's just check out our page to see if we are getting anything. So as you can see, we have Ellen Page, Brad Pitt, Kevin Spacey, Edward Norton, and so on. So we are displaying the list of all the actors that we have. Of course, you can extend this functionality to do whatever you want, but for now, this is just the simple demonstration of how components work and how you can get them set up.\n\n## Review and Next Steps\n\nAlso, if we go right here and I fork this code, so I click that fork icon, now we can see right here the HTML that is getting outputted. So you can change it right here if you want. So we have `for actor in actors` and so on. You can, of course, change it here or use your code editor. Okay, so this is it for this episode. In the next episode, I'm going to show you how you can add more options to your components and, for example, control how many actors get outputted to your page. So you can control the number of actors that get displayed. \n\nSo guys, if you like this video, please give it a thumbs up. If you like the content I put out, maybe, I don't know, subscribe to the channel. Also, you can follow me on Facebook or on Twitter and ask me questions there. Don't forget everything we did here will be available for you on GitHub. The link will be in the description below. And thanks for watching once again, and I'll see you in the next episode."
}
